





DECLARE
	N INT;
	COMMAND VARCHAR2(200);
BEGIN
	COMMAND := 'DROP USER PROYECTOS123 CASCADE';
	SELECT COUNT(*) INTO N
	FROM DBA_USERS
	WHERE USERNAME = 'PROYECTOS123';
	IF ( N = 1 ) THEN
		EXECUTE IMMEDIATE COMMAND;
	END IF;
END;











CREATE USER PROYECTOS123 IDENTIFIED BY holt123;

GRANT CONNECT, RESOURCE TO PROYECTOS123;

ALTER USER PROYECTOS123
QUOTA UNLIMITED ON USERS;

GRANT CREATE VIEW TO PROYECTOS123;

CONNECT PROYECTOS123/holt123










CREATE TABLE TB_MODERADOR(
IDTB_MODERADOR INT PRIMARY KEY,
USUARIO VARCHAR(50),
CLAVE VARCHAR(50),
NOMBRE_APELLIDO VARCHAR(50),
ENTIDAD INT,
TELEFONO INT,
CORREO VARCHAR(45),
DNI INT
);
CREATE TABLE TB_DISTRITO(
IDTB_DISTRITO INT PRIMARY KEY,
NOMBRE VARCHAR(50)
);
CREATE TABLE TB_ZONA(
IDTB_ZONA INT PRIMARY KEY,
NOMBRE VARCHAR(45)
);
CREATE TABLE TB_AGRAVIADOS(
ID_CODIGO INT PRIMARY KEY,
NOMBRE_APELLIDO VARCHAR(50),
DNI INT,
TELEFONO INT,
CELULAR INT,
CORREO VARCHAR(50),
USUARIO VARCHAR(50),
DIRECCION VARCHAR(50),
CLAVE VARCHAR(50)
);
